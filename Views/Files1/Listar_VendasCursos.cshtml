@model IEnumerable<VendaCursoModel>
@using Microsoft.AspNetCore.Http;

@{
    ViewData["Title"] = "Ekutivala";
    ViewContext.RouteData.Values["action"] = "Index";
    Layout = "_Dashboard_Layout";

    
}

<main>
    <div class="container-fluid px-4">
        <h1 class="mt-4">Vendas de Cursos</h1>

        <div class="card mb-4">
            <div class="card-header">
                <i class="fas fa-shopping-cart me-1"></i>
                Lista de Vendas de Cursos
            </div>
            <div class="card-body">
                <table id="VendasCurso" class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>ID</th>
                            <th>Cliente</th>
                            <th>E-mail</th>
                            <th>Telefone</th>
                            <th>Curso</th>
                            <th>Tipo Pagamento</th>
                            <th>Fatura</th>
                            <th>Status</th>
                            <th>Data Cadastro</th>
                            <th>Operações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var venda in Model)
                        {
                            <tr>
                                <td>@venda.IdVenda</td>
                                <td>@venda.NomeCliente</td>
                                <td>@venda.EmailCliente</td>
                                <td>@venda.TelefoneCliente</td>
                                <td>
                                    @if (venda.Curso != null)
                                    {
                                        <span>@venda.Curso.Nome</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">Curso não encontrado</span>
                                    }
                                </td>
                                <td>@venda.TipoPagamento</td>
                                <td>@venda.Fatura</td>
                                <td>
                                    <span class="badge 
                                        @(venda.Status == "Aprovada" ? "bg-success" : 
                                          venda.Status == "Pendente" ? "bg-warning" : 
                                          "bg-danger")">
                                        @venda.Status
                                    </span>
                                </td>
                                <td>@venda.DataCadastro.ToString("dd/MM/yyyy HH:mm")</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a asp-action="Editar_VendaCurso" asp-route-id="@venda.IdVenda" 
                                            class="btn btn-sm btn-warning">
                                            <i class="fas fa-edit"></i> Atualizar
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</main>


@section Scripts {
    <script>
        $(document).ready(function (){
            $('#VendasCurso').DataTable({
                "language": {
                    "emptyTable": "Nenhum vendas cadastrado",
                    "lengthMenu": "_MENU_ resultados por página",
                    "info": "Mostrando _START_ até _END_ de _TOTAL_ vendas",
                    "infoEmpty": "Mostrando 0 até 0 de 0 vendas",
                    "infoFiltered": "(Filtrados de _MAX_ vendas)",
                    "infoPostFix": "",
                    "infoThousands": ".",
                    "loadingRecords": "Carregando...",
                    "processing": "Processando...",
                    "zeroRecords": "Nenhuma venda encontrada",
                    "search": "Pesquisar:",
                    "paginate": {
                        "next": "Próximo",
                        "previous": "Anterior",
                        "first": "Primeiro",
                        "last": "Último"
                    },
                    "aria": {
                        "sortAscending": "Ordenar coluna ascendente",
                        "sortDescending": "Ordenar coluna descendente"
                    }
                },
                "columnDefs": [
                    { "orderable": false, "targets": [6] } // Desabilita ordenação na coluna de operações
                ],
                "order": [[5, "desc"]] // Ordena por data de cadastro (decrescente)
            });
        });
    </script>

}